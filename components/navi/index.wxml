<view class="container">  
  <image bind:tap="onLeft" class="icon" src="{{latest?leftDisImg:leftImg}}" />
  <text class="title">{{title}}</text>
  <image bind:tap="onRight" class="icon" src="{{first?rightDisImg:rightImg}}" />
</view>